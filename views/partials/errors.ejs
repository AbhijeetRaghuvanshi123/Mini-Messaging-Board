<% if (locals.errors && errors.length > 0) { %>
    <div class="error-box">
        <span class="error-icon">!</span>
        <ul class="error-list">
            <% errors.forEach(error => { %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
    </div>
<% } %>